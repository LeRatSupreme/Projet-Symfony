<style>

</style>

<header class="header">
    <nav class="nav">
        <ul>
            <li class="li-nav"><a href="{{ path('app_homedefault') }}">RatusApp</a></li>
            {% if app.user %}
                <li class="li-nav dropdown">
                    <a href="#" class="dropbtn">Authentication</a>
                    <div class="dropdown-content">
                        <a href="#" class="dropbtn">{{ app.user.email }}</a>
                        <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                </li>
            {% else %}
                <li class="li-nav dropdown">
                    <a href="#" class="dropbtn">Authentication</a>
                    <div class="dropdown-content">
                        <a href="{{ path('app_register') }}">Register</a>
                        <a href="{{ path('app_login') }}">Login</a>
                    </div>
                </li>
            {% endif %}
            <li class="li-nav"><a href="{{ path('app_lucky') }}">Lucky</a></li>
            <li class="li-nav"><a href="{{ path('app_about') }}">About</a></li>
            <li class="li-nav dropdown">
                <a href="#" class="dropbtn">Articles</a>
                <div class="dropdown-content">
                    <a href="{{ path('list_articles') }}">List Articles</a>
                    <a href="{{ path('article_new') }}">New Article</a>
                    <a href="{{ path('generate_article') }}">Generate Random Article</a>
                </div>
            </li>
            {% if app.user and not 'ROLE_USER' not in app.user.roles %}
                <li class="li-nav dropdown">
                    <a href="#" class="dropbtn">Admin Panel</a>
                    <div class="dropdown-content">
                        <a href="{{ path('admin_user_list') }}">Manage Users</a>

                    </div>
                </li>
            {% else %}

            {% endif %}
        </ul>
    </nav>
</header>
